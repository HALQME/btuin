# GitHub Packages (npm registry)

`btuin` is published to npmjs.com as an unscoped npm package: `btuin`.

## Install (recommended)

```bash
bun add btuin
```

## About GitHub Packages

GitHub Packages' npm registry only supports scoped packages (`@scope/name`), so an unscoped package name like `btuin` cannot be published there.
